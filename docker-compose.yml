services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: automotive_pieces_auto
    ports:
      - "5432:5432"
    volumes:
      - apa_pgdata:/var/lib/postgresql/data

volumes:
  apa_pgdata:
